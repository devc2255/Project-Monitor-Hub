name: Global Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping-services:
    runs-on: ubuntu-latest
    steps:
      - name: Ping All Services
        run: |
          echo "Pinging Intel Flow Tech..."
          curl -L -A "Mozilla/5.0" -I https://intel-flow-tech.onrender.com/ || true
          
          echo "Pinging Investment Decision Support..."
          curl -L -A "Mozilla/5.0" -I https://huggingface.co/spaces/5axe32/Investment-Decision-Support-System || true
          
          echo "Pinging Intrinsic Valuation Engine..."
          curl -L -A "Mozilla/5.0" -I https://institutional-intrinsic-valuation-engine.onrender.com/ || true
          
          echo "Pinging Core Inventory System..."
          curl -L -A "Mozilla/5.0" -I https://core-inventory-system-rpi1.onrender.com/ || true
          
          echo "Pinging Cyberbullying Detector..."
          curl -L -A "Mozilla/5.0" -I https://cyber-bullying-tweet-detector.onrender.com/ || true
          
          echo "Pinging Data Science Project..."
          curl -L -A "Mozilla/5.0" -I https://datascienceproject-v791.onrender.com/ || true
